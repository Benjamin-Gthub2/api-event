INSERT INTO events (id,
                    type_id,
                    code,
                    name,
                    description,
                    enable,
                    created_at,
                    created_by)
VALUES (?, ?, ?, ?, ?, ?, ?, ?);
